---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Flora.
--- DateTime: 15/08/2018 11:07 PM
---

local WGBM = WarGod.BossMods

local bossString = "Halls of Valor"
print(bossString)
WGBM[bossString] = {}

WGBM[bossString].DPSBlacklist = function(spell, unit, args)
    --print('boo')
    if (unit:AuraRemaining("Water Bubble", "HELPFUL") > 0) then
        printTo(3,'not dpsing with Soul Armor buff')
        return true
    end

    return false
end

--[[WGBM[bossString].DPSWhitelist = function(spell, unit, args)
    local unitid = unit.unitid
    local name = unit.name
    if (name == "Spirit of Gold") then
        return true
    end
end]]

WGBM[bossString].Interrupt = function(spell, unit, args)
    local unitid = unit.unitid
    local name = unit.name
    --[[if UnitCastingInfo(unitid) == "Bone Spike" then
        return
    end]]
    --print('cos interrupt')
    if name == "Valarjar Runecarver" then
        if UnitCastingInfo(unitid) == "Etch" then
            --return (UnitHealth(unitid .. "target") or 0) / (UnitHealthMax(unitid .. "target") or 1) < 0.5
            return true
        end
        return
    elseif name == "Valarjar Thundercaller" then
        if UnitCastingInfo(unitid) == "Thunderous Bolt" then
            return (UnitHealth(unitid .. "target") or 0) / (UnitHealthMax(unitid .. "target") or 1) < 0.5
        end
        return
    end
    return true
end